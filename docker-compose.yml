services:
  z-alpha-securities:
    build: .
    container_name: Z-Alpha-Securities
    ports:
      - "3000:3000"
      - "8000:8000"
    volumes:
      - ./portfolio.db:/app/portfolio.db
    environment:
      - CHOKIDAR_USEPOLLING=true
      - WATCHPACK_POLLING=true
