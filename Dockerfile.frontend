FROM node:18-alpine

WORKDIR /app

COPY frontend/package*.json ./
RUN npm ci

# Source is bind-mounted in dev via compose; copy only to make the image self-contained
COPY frontend/ ./

ENV CHOKIDAR_USEPOLLING=true \
    WATCHPACK_POLLING=true \
    BROWSER=none

EXPOSE 3000
CMD ["npm", "start"]
